<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="160dp"
    android:background="#262626">

    <com.pkmmte.view.CircularImageView
        android:id="@+id/avatar"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/test"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="16dp"
        android:layout_marginStart="16dp"
        android:background="@drawable/round_img_button"
        app:shadow="true" />

    <TextView
        android:id="@+id/since_text"
        android:text="plays since"
        android:layout_toEndOf="@id/avatar"
        android:layout_alignParentBottom="true"
        android:fontFamily="sans-serif-regular"
        android:textColor="#ffffff"
        android:textSize="14sp"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

    <TextView
        android:layout_toEndOf="@id/avatar"
        android:id="@+id/full_name"
        android:text="Andrew Gov"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:layout_above="@id/since_text"
        android:layout_marginStart="12dp"
        android:fontFamily="sans-serif-medium"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

    <TextView
        android:id="@+id/since_date"
        android:text="12.04.32"
        android:layout_toEndOf="@id/since_text"
        android:layout_alignParentBottom="true"
        android:fontFamily="sans-serif-regular"
        android:textColor="#ffffff"
        android:textSize="14sp"
        android:layout_marginStart="5dp"
        android:layout_marginBottom="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />





</RelativeLayout>

